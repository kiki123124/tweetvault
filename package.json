{
  "name": "tweetvault",
  "version": "0.1.0",
  "private": true,
  "description": "Export X (Twitter) bookmarks, AI-classify them, and build an Obsidian knowledge base",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18"
  }
}
